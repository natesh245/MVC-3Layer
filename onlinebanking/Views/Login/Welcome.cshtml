
@{
    ViewBag.Title = "Welcome";
}
@if (Session["Name"] != null)
{


    <div class="welcome-container">
        <ul class="welcome-list">
            <li class="welcome-list-item"> @Html.ActionLink("Account Details", "Details", "Account", new { id = @Session["customerid"] }, new { @class = "welcome-links" })</li>
            <li class="welcome-list-item"> @Html.ActionLink("Customer Details", "Details", "Customer", new { id = @Session["customerid"] }, new { @class = "welcome-links" })</li>
            <li class="welcome-list-item"> @Html.ActionLink("Transaction", "Index", "Transaction", new { id = @Session["customerid"] }, new { @class = "welcome-links" })</li>
        </ul>
        <div><h2 class="welcome-header">Welcome @Session["Name"]</h2></div>

    </div>

}











